# We Fetch the news from different site display here
## Setup Instructions

* Clone the repo
* Make a virtual env and do pip install -r requirements/local.txt
* Start server with python manage.py runserver
